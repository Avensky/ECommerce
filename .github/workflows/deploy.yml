name: Deploy

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request: 
    branches: [ main ]

jobs: 
  getBackend:
    runs-on: self-hosted
    steps:
    - name: Get Code
      uses: actions/checkout@v3
    - name: Use Node
      uses: actions/setup-node@v3
      with:
        node-version: 19.2.0
    - run: |
        npm i
        npm run lint
  getFrontend:
    needs: getBackend
    runs-on: self-hosted
    steps:
    - name: Get Code
      uses: actions/checkout@v3
    - name: Use Node
      uses: actions/setup-node@v3
      with:
        node-version: 19.2.0
    - run: |
        cd frontend
        npm i
        npm run lint
  buildProd:
    needs: getFrontend
    runs-on: self-hosted
    steps:
    - name: Use Node
      uses: actions/setup-node@v3
      with:
        node-version: 19.2.0
    - run: |
        cd frontend
        npm run build
  restartServer:
    needs: buildProd
    runs-on: self-hosted
    steps:
    - name: Get Code
    - run: |
        pm2 stop ECommerce
        pm2 start ECommerce
        pm2 save
        sudo service nginx restart