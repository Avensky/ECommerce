name: Deploy

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request: 
    branches: [ main ]

jobs: 
  getCode:
    runs-on: self-hosted
    steps:
    - name: Get Code
      uses: actions/checkout@v3
    - name: Use Node
      uses: actions/setup-node@v3
      with:
        node-version: 19.2.0
    - run: |
        pm2 stop all
        npm ci
        cd frontend
        npm ci
        npm run build
        pm2 start all
        pm2 save
        sudo service nginx restart